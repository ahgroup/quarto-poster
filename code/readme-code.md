# Code

This folder contains optional code that supports the poster template.

- `make-example-figure.r`: regenerates `figures/example-results.svg` and `figures/example-model-estimates.svg` using base R.

The poster examples are designed to render without running this script first because the generated SVG is committed. Run the script only when updating the example figure.
