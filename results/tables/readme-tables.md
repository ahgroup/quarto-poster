# Tables

This folder contains tables generated by code.

If you use R, some tables may be saved as `.rds` files. For tables that need to
be reviewed outside R, it can be useful to save both an `.rds` file and a
plain-text `.csv` or `.md` companion.

You can create further subfolders if that makes sense.
