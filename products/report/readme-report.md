# Report

This folder contains an example report written as a Quarto document with HTML
output.

The report pulls in generated results, such as tables and figures, from the
`results/` folder. Rerender the report after changing the report source or any
results it displays.

The example report includes an optional software/session information section using base R `sessionInfo()`. Keep, remove, or adapt that section depending on the project needs.
